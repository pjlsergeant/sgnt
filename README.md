# SGNT - Simple TypeScript building blocks for LLM-based apps

Simple TypeScript building blocks for LLM-based apps

API is rapidly changing, don't use this yet.

## Known Bugs

Doesn't seem to work well with TypeBox enums (literal-unions are fine tho)